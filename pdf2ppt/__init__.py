"""Vendored PDF2PPT parser subset for exam-to-html.

Only the v2 parser files needed by topic_garden.ingest are copied here so
`from pdf2ppt...` resolves inside this repository instead of a sibling checkout.
"""
